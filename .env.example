# Base URL of the PHP backend (see /backend). Point this at wherever XAMPP serves the "backend" folder from.
# Check your actual Apache port in xampp/apache/conf/httpd.conf ("Listen" line) — it isn't always 80.
VITE_API_BASE_URL=http://localhost:8081/nethras-backend
